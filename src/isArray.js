// @flow

/**
 * Returns true if {@link obj} is an array, if not false.
 * This function is just an alias for Array.isArray.
 * @param {obj} - The object to check.
 */
function isArray(obj:any) {
  return Array.isArray(obj);
}

export {isArray};
